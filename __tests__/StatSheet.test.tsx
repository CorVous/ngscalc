import '@testing-library/jest-dom'
import { render, screen } from '@testing-library/react'
import StatSheet from '../components/StatSheet'

describe.skip("StatSheet component", () => {

  beforeEach(() => {
      render(<StatSheet />)
  })
  
  it("should render the StatSheet component", () => {
    
  })
})